#Every CMakeLists file should have at his root a defined minimum version
#this is for CMake to recognize from where to start working on
cmake_minimum_required(3.8)

#We must define a project name which is gonna be used through our project to add executables and other stuff
project(FirstProject)